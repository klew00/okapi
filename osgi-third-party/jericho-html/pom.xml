<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>net.htmlparser.jericho</groupId>
    <artifactId>jericho-html-osgi</artifactId>
	<version>3.1.0</version>
    <name>Jericho OSGi Bundle</name>
	<packaging>bundle</packaging>
	<build>
    <plugins>
		<plugin>
			<groupId>org.apache.maven.plugins</groupId>
			<artifactId>maven-compiler-plugin</artifactId>
			<version>2.0.2</version>
			<configuration>
				<source>1.5</source>
				<target>1.5</target>
			</configuration>
		</plugin>
	
      <plugin>
        <groupId>org.apache.felix</groupId>
        <artifactId>maven-bundle-plugin</artifactId>
        <version>1.2.1</version>
        <extensions>true</extensions>
        <!--
          the following instructions build a simple set of public/private classes into an OSGi bundle
        -->
        <configuration>
	    <unpackBundle>true</unpackBundle>
            <!--
              embed compile/runtime dependencies using path that matches the copied dependency folder
            <Embed-Dependency>*;scope=compile|runtime;inline=false</Embed-Dependency>
            <Embed-Directory>target/dependency</Embed-Directory>
            <Embed-StripGroup>true</Embed-StripGroup>
          <manifestLocation>META-INF</manifestLocation>
          -->
          <instructions>
			<Bundle-RequiredExecutionEnvironment>J2SE-1.5</Bundle-RequiredExecutionEnvironment>
            <Bundle-SymbolicName>${pom.groupId}</Bundle-SymbolicName>
            <Bundle-Version>${pom.version}</Bundle-Version>
            <!--
              assume public classes are in the top package, and private classes are under ".internal"
            -->
            <Export-Package>${pom.groupId}.*,${pom.groupId}.nodoc.*;version="${pom.version}"</Export-Package>
<!--
            <Private-Package>${bundle.namespace}.internal.*</Private-Package>
-->
          </instructions>
        </configuration>
      </plugin>
    </plugins>
</build>
<dependencies>
    <dependency>
        <groupId>net.htmlparser.jericho</groupId>
        <artifactId>jericho-html</artifactId>
        <version>3.1</version>
    </dependency>
</dependencies>
</project>
